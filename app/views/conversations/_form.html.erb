<%= form_with(model: conversation) do |form| %>
  <% if conversation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
        <% conversation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :user_a_id, style: "display: block" %>
    <%= form.number_field :user_a_id %>
  </div>

  <div>
    <%= form.label :user_b_id, style: "display: block" %>
    <%= form.number_field :user_b_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
